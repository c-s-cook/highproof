{
  "name": "hpp-confirmation-api",
  "version": "1.0.0",
  "description": "API for retrieving order details & voucher codes after Stripe purchase.",
  "main": "index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "c-s-cook",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.15.3",
    "dotenv": "^16.5.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.799.0"
  }
}
